[workspace]
resolver = "2"
members = [
    "crates/solpaper-app",
    "crates/solpaper-core",
    "crates/solpaper-windows",
    "crates/solpaper-storage",
]
# Disposable spikes stay outside the production workspace.
exclude = ["spikes/desktop-overlay"]

[workspace.package]
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false
rust-version = "1.80"

[workspace.dependencies]
solpaper-core = { path = "crates/solpaper-core" }
solpaper-storage = { path = "crates/solpaper-storage" }
solpaper-windows = { path = "crates/solpaper-windows" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
windows = { version = "0.58", features = [
    "Win32_Foundation",
    "Win32_Graphics_Gdi",
    "Win32_Security",
    "Win32_System_LibraryLoader",
    "Win32_System_Threading",
    "Win32_UI_HiDpi",
    "Win32_UI_WindowsAndMessaging",
] }
